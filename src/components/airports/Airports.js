import { BREADCRUMB_OPTIONS } from "../../constants/breadcrumb-constants";
import HorizontalPanel from "../panel/HorizontalPanel";

export const Airports = () => {
  return (
    <div>
      <HorizontalPanel />
      dsdsd
    </div>
  );
};
